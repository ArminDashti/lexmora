# Quiz: English prompt / Persian guess — deploy T3 (2026-08-28)

## Changes

- API quiz pool: `en_fa` + `fa_en` translate history only
- API maps every question to English prompt + Persian answer (`fa_en` swaps input/result)
- WebUI Quiz: always shows Next, Delete, Answer buttons; Answer reveals Persian

## Deploy

- API: `run-on-docker-server.ps1` → `/cloud-admin/docker/lexmora` (lexmora-api:latest)
- WebUI: rebuilt locally; replaced stale `lexmora-webui` container on `/home/cloud-admin/docker/lexmora`
- SSH: port 22 (`ssh t3`); port 80 refused during deploy

## Verify

- https://lexmora.xaigrok.ir → 200
- https://lexmora-api.xaigrok.ir/api/v1/health → 200
- Containers: lexmora-api, lexmora-pgsql, lexmora-webui up on t3-net
