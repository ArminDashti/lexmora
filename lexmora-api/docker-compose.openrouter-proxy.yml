# Server overlay: route OpenRouter via 3proxy in windscribe netns (same as SoftEther).
services:
  lexmora-api:
    environment:
      OPENROUTER_HTTP_PROXY: ${OPENROUTER_HTTP_PROXY:-http://windscribe:8778}
