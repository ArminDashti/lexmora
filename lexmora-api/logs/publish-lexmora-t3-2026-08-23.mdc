# Publish Lexmora on Irancell-T3 (2026-08-23)

## Goal

Redeploy `lexmora-api` + `lexmora-webui` after History multi-type + Quiz features.

## Stacks

| Stack | Image | Domain | Backend | Volume dir |
|-------|-------|--------|---------|------------|
| `lexmora` | `lexmora-api:latest` | `https://lexmora-api.xaigrok.ir` | `lexmora-api:8080` on `t3-net` | `/cloud-admin/docker/lexmora` |
| `lexmora-webui` | `lexmora-webui:latest` | `https://lexmora.xaigrok.ir` | `lexmora-webui:80` on `t3-net` | `/home/cloud-admin/docker/lexmora-webui` |

## Steps

1. Fixed `$args` StrictMode crash in both `run-on-docker-server.ps1` scripts (`UnboundArguments`).
2. Deployed API via `.armin/docker-scripts/run-on-docker-server.ps1` (local build → SCP → load → compose up).
3. Added `@types/node` so Docker `vue-tsc` build succeeds for webui.
4. Webui compose conflict: orphan `lexmora-webui` left by API project — `docker rm -f` then compose up.
5. Verified LE PEMs already present; issuer Let's Encrypt; SoftEther SNI still OK.

## Verify

- `lexmora.xaigrok.ir` → HTTP 200, issuer Let's Encrypt YE2
- `lexmora-api.xaigrok.ir/api/v1/health` → HTTP 200 `{"status":"ok"}`, issuer Let's Encrypt YE1
- `ovpbackup.xaigrok.ir` SoftEther cert subject still present (passthrough)
- Containers: `lexmora-webui`, `lexmora-api`, `lexmora-pgsql` Up on `t3-net`

## Notes

- DNS both hostnames → `2.144.27.124`
- No HAProxy config change (routes already present)
- Certs valid until ~2026-10-22; renew hook not re-touched
