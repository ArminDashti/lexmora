# Lexmora instruction redesign deploy

Timestamp: 2026-09-08 14:59:51 (local) / ~11:22–11:29 UTC on host

## Commands (summary)

- Local `docker build` for `lexmora-api:latest` and `lexmora-webui:latest`
- SCP image tars over SSH port 443 to `/tmp/`
- API stack: `/cloud-admin/docker/lexmora` compose down → load image → up -d
- WebUI: `/home/cloud-admin/docker/lexmora` recreate `lexmora-webui`
- DB smoke: 40 instruction keys; history_type includes `cursor`, `frontend`
- Options API: translate scientific[9 topics], cursor skill/agent, frontend for-agent
- SQL replace for scientific opener grammar on live instructions

## Result

- `lexmora-api`, `lexmora-pgsql`, `lexmora-webui` running
- SoftEther / other stacks not touched

## Warnings

- Compose warns orphan containers across split API vs WebUI project dirs (pre-existing layout)
- `DEFAULT_PASSWORD` env on API does not match live user password (login still works with existing DB hash)
